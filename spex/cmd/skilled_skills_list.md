# skilled skills list

List skills in currently used skills repository.

## Options

`--details`: Displays additional metadaya details.

Currently only metadata supported is `updated` which indicates when given skill repository was updated (if skill is Git submodule, defaults to `-` otherwise). Updated is displayed is human-friendly format (`1d ago`, etc).